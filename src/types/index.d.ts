interface ClassesType {
  main: string,
  app: string,
  footer: string
  cardMedia: string,
  cardContent: string,
  cardActions: string,
  cardGrid: string,
  heroContent: string,
  card: string,
}

enum MembershipType {
  Basic = "Basic",
  Premium = "Premium"
}

interface ReferenceData {
  code: string,
  description: string
}