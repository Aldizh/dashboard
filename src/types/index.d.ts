interface ClassesType {
  app: string,
  footer: string
  cardMedia: string,
  cardContent: string,
  cardActions: string,
  cardGrid: string,
  heroContent: string,
  card: string,
}

interface ReferenceData {
  code: string,
  description: string
}

enum MembershipType {
  Basic = "Basic",
  Premium = "Premium"
}